# 项目整体评估 v1 — 2026-08-02

> 首篇 devlog。本次评估由 Claude Code（deepseek-v4-flash）执行，覆盖项目全部核心资产。

## 1. 评估范围

| 维度 | 内容 |
|------|------|
| 写作体系 | 4 个 Skill（math-writing / manage / agenda / writing-interaction） |
| 工作流 | euclids-gate-workflow-v5（README、索引、四个 skill 源） |
| 文章 | 根目录 16 篇正文 + 4 个 No16 变体 + 归档 11 份版本 |
| 数据 | article_index.json（13 条记录 + agenda） |
| 基建 | .claude/ 配置（mcp.json、settings.json）、git 历史 |
| 精读文章 | No1 / No10 / No15 / No16（含对话版、荷马体、实验稿） |

## 2. 项目快照

- 已发布文章：No1–No16（编号有缺口的为实际不存在或未纳入索引的记录）
- 四大板块：代数、几何、组合、数论均有覆盖，另有「方法」类文章（No14/No15）
- 工作流版本：v5.2（生产就绪）
- git：本地 main 分支，远程 GitHub 公开仓库（远程无密钥泄露，详见 P0）
- 待办：agenda.pending_topics =「最后通牒博弈」（No16 预告）

## 3. 发现（分级）

### P0 — 紧急

**API 密钥明文进入 git 跟踪**
- `.claude/mcp.json` 与 `.claude/settings.json` 被 git 跟踪，含 4 个服务的 API 密钥（DeepSeek / 豆包 / 魔搭 / md2card）
- 已核实：含密钥的提交（37beb4c）**未推送**到远程公开仓库；魔搭密钥在本地历史中，其余 3 个仅在工作区
- 风险：任何一次 `git add .` + push 即全部公开（远程仓库为 PUBLIC）
- 违反 CLAUDE.md 自身的 Git 规范「不要在提交中包含 API key 等敏感信息」

### P1 — 重要

**1. 文章索引与磁盘状态严重脱节**
- 索引缺失 3 条记录：No10 习题总解、No13 群作用、No14 构造性证明（文件均在磁盘上）
- 前 12 篇的 file 路径指向已不存在的 Marvis AppData 目录
- agenda.pending_answers 缺 id 5/6/8/9/12（对应文章思考题均为未解答状态）
- manage / agenda 两个 skill 中的索引路径同样是旧路径

**2. Skill 引用旧环境的工具名**
- 四个 SKILL.md 使用 `write_file` / `ask_user` / `read_text` / `yyb-product 卡片` / `engram_remember` 等 Marvis 环境工具名，在 Claude Code 环境中不存在（对应物：Write / AskUserQuestion / Read）

**3. 双份 skill 拷贝已分叉**
- `euclids-gate-workflow-v5/`（源）与 `.claude/skills/`（生效副本）内容不同步：.claude/skills 版已有「板块轮换硬检查（1.1a）」，源目录 README 更新日志停在 v5.1

**4. 根目录文件混杂**
- 4 个 No16 变体（主版/对话版/实验稿/荷马体）全部未提交，最终版未定
- workflow-v5 源目录与 zip 压缩包停留在根目录

### P2 — 一般

- No1/No2/No3/No13 及归档 5 份文件残留 AIGC 水印（frontmatter + 页尾声明）
- settings.json allow 列表堆积历史实验残留命令（含密钥的 curl 调用）

## 4. 亮点（值得保留的设计）

1. **两阶段门控流程**：头脑风暴允许越界创意 + 批准后零偏差执行，有效抑制 AI 写作失控
2. **写作规范落地为内容**：精神纲领六条、「何以所以然」宗旨、数学史 300-500 字要求、中英混排规范均体现在成品中
3. **No15/No16 质量标杆**：No15 以「有 vs 在哪」统一四种非构造方法；No16 的 δ > 2/3 推导与 Axelrod 史实严谨，思考题分级（★）设计出色
4. **归档机制执行到位**：archive 内 v1-v4 版本备份完整、命名规范
5. **风格探索有价值**：对话版、荷马体是文风实验的成功方向

## 5. 决策记录（本次评估用户拍板）

| # | 决策 | 选项 | 选择 |
|---|------|------|------|
| 1 | 索引唯一真源位置 | 仓库根 / workflow-v5 | **仓库根目录** |
| 2 | No16 最终发布版 | 主版 / 对话版 / 全保留 | **对话版** |
| 3 | 水印清理范围 | 仅当前文件 / 连 archive | **连 archive 一并清理** |
| 4 | 密钥处理 | 轮换+移出跟踪 / 仅移出跟踪 | **轮换 + 移出跟踪** |

## 6. 行动项

- [x] 建立 devlog 目录与规范（本次）
- [x] 密钥：git rm --cached + .gitignore + settings.local.json（用户侧需轮换 4 个 key）
- [x] 索引：迁移根目录、补齐缺失记录、修正路径、同步 agenda
- [x] skill：备份后适配 Claude Code 工具名
- [x] 归置：workflow-v5 入 archive；No16 对话版为正式版，其余入 archive
- [x] 水印：根目录 4 个 + 归档 8 个文件清理，全库零残留
- [x] settings.json：清理历史实验 allow 条目
- [ ] 用户侧：4 个平台轮换 API 密钥并写入 settings.local.json（push 前必须完成——历史提交含魔搭 key，一旦 push 即公开）
- [ ] 后续：完善工作流（沉淀对话体/荷马体文体模板）、写新文章（最后通牒博弈）

## 7. 结语

项目处于「高质量内容 + 半自动流程」状态：写作引擎与文章本体是强项，数据层与配置层的漂移是当前主要风险。本次修复后，项目应转入稳定期，devlog 将按里程碑持续记录。
